# salesforce-setup-audit-package-generator
A Salesforce tool that analyzes Setup Audit Trail changes, extracts metadata component names, and generates a deployable package.xml for selective metadata deployment.
